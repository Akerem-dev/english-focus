/** Current content contract version for a single vocabulary entry. */
export const VOCABULARY_SCHEMA_VERSION = "1.0.0" as const;

export type VocabularySchemaVersion = typeof VOCABULARY_SCHEMA_VERSION;
