# Core Pack Strategy

A future 5,000-entry core pack is installed separately from user content.

Layers:

- core entry
- user-created entry
- user override
- user metadata

A user override must not mutate the original core pack. User metadata must survive entry replacement.
