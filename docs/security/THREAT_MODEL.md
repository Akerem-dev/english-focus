# Threat Model

Inputs: search text, clipboard text, JSON files, pack files, backup files, notes, tags, and settings.

Controls: file-size limits, unknown-data parsing, schema validation, semantic validation, no HTML rendering, minimal Tauri permissions, transaction-safe import, backup before migration, normalized file paths, and redacted diagnostics.
