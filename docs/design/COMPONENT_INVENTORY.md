# Component Inventory

App shell, navigation, top bar, command bar, search input, word header, metadata row, section, accordion, definition list, grammar table, example list, library row, filters, selection toolbar, quick preview, instruction viewer, paste editor, validation list, preview, duplicate comparison, settings fields, empty state, skeleton, inline error, toast, confirm dialog, import summary, and diagnostics row.
